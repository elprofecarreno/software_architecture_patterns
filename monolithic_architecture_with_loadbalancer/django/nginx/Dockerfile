# IMAGEN A USAR JUNTO A LA VERSION (LATEST:ULTIMA VERSION)
FROM nginx:latest
# PUERTO DE COMUNICACION DE APACHE 2
EXPOSE 80
# COPIAR nginx.conf EN EL CONTENEDOR DENTRO DE LA CARPETA /etc/nginx/
COPY nginx.conf /etc/nginx/nginx.conf